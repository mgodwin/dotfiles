---

# Need to install git from homebrew, for latest/greatest version, but also b/c
# this installs the bash command completion as well.
- name: Install git
  homebrew:
    name: git
    state: present

- name: Setup gitconfig file
  copy:
    src: gitconfig
    dest: ~/.gitconfig
    mode: 0755
